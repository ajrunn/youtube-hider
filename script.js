const query = document.querySelectorAll('div#channel-info.style-scope.ytd-video-renderer, span.inline-metadata-item.style-scope.ytd-video-meta-block');

query.forEach(elem => elem.style.display = 'none');